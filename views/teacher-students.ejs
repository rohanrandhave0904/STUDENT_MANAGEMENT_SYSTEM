<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Your head content here -->
</head>
<body>
    <h1>Students for Teacher: <%= teacherName %></h1>
    <ul>
        <% students.forEach(student => { %>
            <li><%= student.name %></li>
        <% }); %>
    </ul>
</body>
</html>
